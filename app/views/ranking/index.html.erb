<h2>Ranking de Projetos</h2>

<h4 class="mt-4">🔥 Mais Curtidos</h4>
<ul>
  <% @most_liked.each do |project| %>
    <li>
      <%= link_to project.title,
          public_project_path(
            username: project.user.username,
            project_slug: project.slug
          ) %>
      (❤️ <%= project.likes_count %>)
    </li>
  <% end %>
</ul>

<h4 class="mt-5">👁 Mais Vistos</h4>
<ul>
  <% @most_viewed.each do |project| %>
    <li>
      <%= link_to project.title,
          public_project_path(
            username: project.user.username,
            project_slug: project.slug
          ) %>
      (👁 <%= project.views_count %>)
    </li>
  <% end %>
</ul>
