<h2>🏆 Ranking de Projetos</h2>

<h4 class="mt-4">🔥 Trending (últimos 7 dias)</h4>
<ul>
  <% @trending.each do |project| %>
    <li>
      <%= link_to project.title,
        public_project_path(username: project.user.username, project_slug: project.slug) %>
      <small class="text-muted">
        (Score: <%= project.engagement_score %>)
      </small>
    </li>
  <% end %>
</ul>

<h4 class="mt-5">👑 All-time</h4>
<ul>
  <% @all_time.each do |project| %>
    <li>
      <%= link_to project.title,
        public_project_path(username: project.user.username, project_slug: project.slug) %>
      <small class="text-muted">
        (Score: <%= project.engagement_score %>)
      </small>
    </li>
  <% end %>
</ul>
